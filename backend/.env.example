# Server Configuration
NODE_ENV=development
PORT=3000

# Google OAuth Configuration (Web Client for Backend)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/callback

# Google OAuth Mobile Client IDs (for reference)
GOOGLE_IOS_CLIENT_ID=your_ios_client_id_here
GOOGLE_ANDROID_CLIENT_ID=your_android_client_id_here

# JWT Configuration (IMPORTANT: Use strong secrets in production)
JWT_SECRET=your_jwt_secret_here_must_be_at_least_32_characters_long_for_security
JWT_EXPIRES_IN=24h
JWT_ISSUER=assistantapp
JWT_AUDIENCE=assistantapp-client

# Logging Configuration
LOG_LEVEL=info
LOG_FILE_PATH=./logs/app.log

# CORS Configuration (comma-separated list)
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Security Configuration
BCRYPT_SALT_ROUNDS=12

# Database Configuration (for future use)
# DATABASE_URL=postgresql://user:password@localhost:5432/assistantapp
# REDIS_URL=redis://localhost:6379

# Production Security Notes:
# 1. JWT_SECRET should be at least 64 characters in production
# 2. Use HTTPS URLs for CORS_ORIGIN in production
# 3. Set NODE_ENV=production
# 4. Use secure database connections
# 5. Consider using environment-specific config files